server:
  port: 8080
spring:
  application:
    name: gateway-service
  cloud:
    config:
      uri: http://${CONFIGURATION_HOST:localhost}:${CONFIGURATION_PORT:8888}
      fail-fast: false
      label: ""
